// eslint-disable-next-line react/prop-types
function WayToTech({ title, description }) {
  return (
    <li>
      <p>
        <strong>{title}</strong>
        {description}
      </p>
    </li>
  )
}

export default WayToTech
